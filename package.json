{
  "name": "learnreactnative",
  "version": "1.0.0",
  "private": true,
  "description": "React Native monorepo using pnpm workspaces",
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.{js,jsx,json,ts,tsx}": [
      "prettier --write"
    ]
  }
}
